<h1>Nouvelle categorie</h1>

<%= form_for @category do |f| %>

<div class="form-group">
    <%= f.label :label, "Label de la catégorie" %>
    <%= f.text_field :label, placeholder:"titre", class: "form-control" %>
</div>

<div class="form-group">
    <%= f.label :contenu, "Contenu de la catégorie" %>
    <%= f.text_area :content, placeholder:"fonction", class: "form-control" %>
</div>

<div class="form-group">
    <button type="submit" class="btn btn-primary">Mettre à jour la categorie</button>
</div>

<% end %>